import {describe,expect,it} from "vitest";import {calculateSimulation} from "@/lib/finance/calculator";
describe("calculator",()=>{it("calculates intermediate and final values",()=>expect(calculateSimulation([{id:"a",amount:5000,sortOrder:1000},{id:"b",amount:-1500,name:"Rent",sortOrder:2000},{id:"c",amount:-300,name:"Groceries",sortOrder:3000},{id:"d",amount:750,name:"Side Income",sortOrder:4000},{id:"e",amount:-200,name:"Equipment",sortOrder:5000}]).map(x=>x.runningValue)).toEqual([5000,3500,3200,3950,3750]));it("recalculates after reorder",()=>expect(calculateSimulation([{id:"a",amount:5000,sortOrder:1000},{id:"b",amount:-1000,sortOrder:3000},{id:"c",amount:500,sortOrder:2000}]).map(x=>x.runningValue)).toEqual([5000,5500,4500]))});
