"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Calculator,CircleDollarSign,Settings} from "lucide-react";import {cn} from "@/lib/utils";
export function Dock(){const pathname=usePathname();const items=[{href:"/cash-flow",label:"Cash Flow",icon:CircleDollarSign},{href:"/calculator",label:"Calculator",icon:Calculator},{href:"/settings",label:"Settings",icon:Settings}];return <nav className="fixed inset-x-4 bottom-4 z-50 mx-auto flex max-w-md items-center justify-around rounded-2xl border border-[var(--border)] bg-[var(--card)]/95 p-2 shadow-lg backdrop-blur">{items.map(({href,label,icon:Icon})=><Link key={href} href={href} className={cn("flex min-h-12 min-w-20 flex-col items-center justify-center gap-1 rounded-xl px-2 text-xs text-[var(--muted)]",pathname.startsWith(href)&&"bg-black/5 text-[var(--foreground)] dark:bg-white/10 dark:text-white")}><Icon size={18}/>{label}</Link>)}</nav>}
