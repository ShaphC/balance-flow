"use client";
import {useState} from "react";import {Button} from "@/components/ui/button";import {createClient} from "@/lib/supabase/client";
export function SettingsForm(){const [privacy,setPrivacy]=useState(false);const supabase=createClient();async function logout(){await supabase.auth.signOut();window.location.href="/login"}return <div className="space-y-4"><div className="rounded-2xl border border-[var(--border)] bg-[var(--card)] p-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-medium">Test / Privacy Mode</h2><p className="mt-1 text-sm text-[var(--muted)]">Masks financial values for screen sharing and tester feedback. Stored data is never changed.</p></div><Button variant={privacy?"default":"outline"} onClick={()=>setPrivacy(v=>!v)}>{privacy?"On":"Off"}</Button></div>{privacy&&<p className="mt-4 rounded-xl bg-black/5 p-3 text-sm dark:bg-white/10">🔒 Privacy Mode is active.</p>}</div><Button variant="outline" onClick={logout}>Log out</Button></div>}
